#ifndef HEADER_HPP
#define HEADER_HPP

#include<irrlicht.h>

#if defined(_MSC_VER)
	#pragma comment(lib, "Irrlicht.lib")
#endif

using namespace irr;
using namespace core;
using namespace video;
using namespace scene;
using namespace gui;
using namespace io;

void testLoop();
#endif
